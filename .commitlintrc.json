{
  "extends": ["@commitlint/config-conventional"],
  "rules": {
    "type-enum": [2, "always", ["feat", "fix", "hotfix", "refactor"]],
    "type-empty": [2, "never"],
    "scope-enum": [2, "always", ["home-page", "style", "settings", "nx"]],
    "scope-empty": [2, "never"]
  }
}
